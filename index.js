const expect = require("expect");

1 + 80; 
60 - 40; 
2 * 3.4; 
5.0 / 2.5; 

function add (a,b) {
    return a + b
}
add (1,80)
function subtract (c,d) {
    return c - d
}
subtract (60,40)
function multiply (e,f) {
    return e * f
}
multiply (2,3.4)
function divide (g,h) {
    return g / h
}
divide (5.0,2.5)

function increment (z) {
    return z += 1 
}
increment (4);

function decrement (t) {
    return t -= 1;
}
decrement (12);



function makeInt (n,k) {
    return (n.toString());
}
makeInt ("4",10);

function isNaN (preserveDecimal) {
    return true;
}
isNaN ('2.222');




